{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "npx tailwindcss build -i ./src/input.css -o ./public/styles.css --minify",
    "watch:css": "npx tailwindcss -i ./src/input.css -o ./public/styles.css --watch --minify",
    "build:js": "npx esbuild ./src/index.js --outfile=public/script.js --bundle --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "alpinejs": "^3.13.10",
    "esbuild": "0.21.3",
    "tailwindcss": "^3.4.3"
  }
}
